# Core ML
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.28.0
safetensors>=0.4.0
sentencepiece>=0.1.99

# Training
tqdm>=4.65.0
numpy>=1.24.0

# Optional: Flash Attention for faster training (recommended for RTX 6000)
# flash-attn>=2.0.0
